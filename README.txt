studio -15
server -14 localdb

count files for load ->class constants
cookies data- separate '@'

[HttpPost] \[HttpGet] - before method - only post\get request
 return PartialView(); - partial
return RedirectToAction("Index"); - redirect








----------------------
.OrderBy(x => x.Id).SkipWhile(x1 => x1.Id != startId)    => where(x1=>x1.Id>startId)


